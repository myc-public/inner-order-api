// app parameters
application.name=inner-order-api
application.groupId=ma.company.inner.socle
git.url=https://github.com/myc-public/inner-order-api.git

// Contact Ops team to get available port
application.port=8080

// NOTE: parametres de deploiement (server.*, artifactory.*) retires pour l'instant.
// A renseigner avec les vraies valeurs quand le pipeline de deploiement sera repris.
